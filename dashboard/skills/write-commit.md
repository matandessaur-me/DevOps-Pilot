---
name: write-commit
description: Produce a concise conventional-style commit message from the current working-tree changes, without actually committing.
---

# Write commit

1. Run `Show-Diff.ps1 -Repo '<active repo>'` to see the changes.
2. Summarize in 1-2 sentences focusing on the WHY, not the WHAT.
3. Output the final message inside a code block. Do not commit.
